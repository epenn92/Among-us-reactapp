import React from 'react'


const Character = ( { char } ) => {
    return (
        <div className = 'character'>
            
            {/* <li>{char.map(e => (
                <h3 key={e._id}>
                    {e.name}
                    </h3>
                ))}</li> */}
        </div>
    )
}

export default Character
